'use strict';

let tail    = require('./tail');

module.exports = apart;

function apart(fn) {
    let first = tail(arguments);
    
    return function() {
        let args    = [...first, ...arguments];
        
        return fn(...args);
    };
}
